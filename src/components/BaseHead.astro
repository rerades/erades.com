---
/**
 * @component BaseHead
 * @title Base Head
 * @description Todo lo que va en el <head> de cualquier página: metadatos globales, canónica, Open Graph, Twitter Card, los tres feeds RSS y Google Analytics.
 *
 *
 * @usage
 *
 * ```astro
 * ---
 * import BaseHead from "./BaseHead.astro";
 * ---
 *
 * <head>
 *   <BaseHead title={title} description={description} image={heroImage} />
 * </head>
 * ```
 */
import { SITE_TITLE } from "../consts";
import GoogleAnalytics from "./GoogleAnalytics.astro";
import ViewTransitions from "./ViewTransitions.astro";

interface Props {
  title: string;
  description: string;
  image?: string;
}

const canonicalURL = new URL(Astro.url.pathname, Astro.site);

const { title, description, image = "/blog-placeholder-1.jpg" } = Astro.props;
---

<!-- Global Metadata -->
<meta charset="utf-8" />

<!--
  El tema se aplica aquí, en el <head>, y no desde ThemeToggle: en el <body>
  llegaba un frame tarde, y con view transitions eso significa que el snapshot
  de salida y el de entrada pueden diferir en tema durante ese frame.
-->
<script is:inline>
  if (localStorage.getItem("theme") === "dark") {
    document.documentElement.classList.add("dark");
  }
</script>
<meta name="viewport" content="width=device-width,initial-scale=1" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="sitemap" href="/sitemap-index.xml" />

<!--
  Las dos caras que se usan por encima del pliegue en todas las páginas: Raleway
  para el cuerpo, Inter para el nav. Van precargadas porque el `font-display:
  optional` de global.css solo usa la fuente si llega dentro del bloqueo
  inicial; sin esto, en un runner de CI no llegaba y la página entera se
  pintaba con la tipografía del sistema. `crossorigin` es obligatorio para
  fuentes aunque sean del mismo origen: sin él el navegador descarga el fichero
  dos veces.
-->
<link
  rel="preload"
  href="/fonts/Raleway-VariableFont_wght.woff2"
  as="font"
  type="font/woff2"
  crossorigin
/>
<link
  rel="preload"
  href="/fonts/Inter-Medium.woff2"
  as="font"
  type="font/woff2"
  crossorigin
/>

<!-- RSS Feeds -->
<link
  rel="alternate"
  type="application/rss+xml"
  title="RSS Feed (Español)"
  href={new URL("rss", Astro.site)}
/>
<link
  rel="alternate"
  type="application/rss+xml"
  title="RSS Feed (English)"
  href={new URL("en/rss", Astro.site)}
/>
<link
  rel="alternate"
  type="application/rss+xml"
  title="RSS Feed (Todos)"
  href={new URL("feed", Astro.site)}
/>

<meta name="generator" content={Astro.generator} />

<!-- Canonical URL -->
<link rel="canonical" href={canonicalURL} />

<!-- Primary Meta Tags -->
<title>{title}</title>
<meta name="title" content={title} />
<meta name="description" content={description} />

<!-- Open Graph / Facebook -->
<meta property="og:type" content="website" />
<meta property="og:url" content={Astro.url} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={new URL(image, Astro.url)} />

<!-- Twitter -->
<meta property="twitter:card" content="summary_large_image" />
<meta property="twitter:url" content={Astro.url} />
<meta property="twitter:title" content={title} />
<meta property="twitter:description" content={description} />
<meta property="twitter:image" content={new URL(image, Astro.url)} />

<ViewTransitions />

<!-- Google Analytics - Non-blocking -->
<GoogleAnalytics />
