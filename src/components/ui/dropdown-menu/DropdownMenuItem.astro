---
// Generated from bejamas-juno style registry. Do not edit packages/ui/src/components/dropdown-menu/DropdownMenuItem.astro directly.
import type { HTMLAttributes } from "astro/types";
import { cn } from "@/lib/utils";

interface Props extends HTMLAttributes<"div"> {
  disabled?: boolean;
  inset?: boolean;
  value?: string;
  variant?: "default" | "destructive";
  class?: string;
  /**
   * Añadido sobre el registry: con `href` el ítem se renderiza como <a>. Un
   * <div role="menuitem"> pierde clic central, ctrl+clic y "abrir en pestaña
   * nueva", y el runtime localiza los ítems por `data-slot`, no por etiqueta.
   */
  href?: string;
  target?: string;
  rel?: string;
}

const {
  disabled = false,
  inset = false,
  value,
  variant = "default",
  class: className = "",
  href,
  target,
  rel,
  ...rest
} = Astro.props as Props;

const Tag = href ? "a" : "div";
---

<Tag
  {...rest}
  href={href}
  target={target}
  rel={rel}
  role="menuitem"
  tabindex={disabled ? -1 : 0}
  data-slot="dropdown-menu-item"
  data-value={value}
  data-disabled={disabled ? "" : undefined}
  data-inset={inset ? "" : undefined}
  data-variant={variant}
  class={cn(
    "focus:bg-accent focus:text-accent-foreground data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive/10 dark:data-[variant=destructive]:focus:bg-destructive/20 data-[variant=destructive]:focus:text-destructive data-[variant=destructive]:*:[svg]:text-destructive not-data-[variant=destructive]:focus:**:text-accent-foreground gap-2 rounded-sm px-2 py-1.5 text-sm data-inset:pl-8 [&_svg:not([class*='size-'])]:size-4",
    "group/dropdown-menu-item relative flex cursor-default items-center outline-hidden select-none data-[disabled]:pointer-events-none data-[disabled]:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0",
    className,
  )}
>
  <slot />
</Tag>
