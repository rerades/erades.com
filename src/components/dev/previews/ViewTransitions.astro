---
// No renderiza nada: solo instala listeners en el <head>. Meterlo aquí de
// verdad duplicaría esos listeners en la página de docs, así que se describe.
---

<p class="text-sm text-muted-foreground">
  Sin salida visible. Marca el título de la tarjeta pulsada con
  <code>view-transition-name: post-title</code> para que se empareje con el
  <code>h1</code> del post al navegar.
</p>
